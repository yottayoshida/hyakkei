// Placeholder for the Renderer, query engine glue, DataSource layer,
// guideline engine, and bake() (ARCHITECTURE §2/§9, ADR-0005). Real content
// lands in M1 (issues #6, #8, #9) and M2 (#13).
import { SCHEMA_PACKAGE_VERSION } from "@hyakkei/schema";

export const CORE_PACKAGE_VERSION = SCHEMA_PACKAGE_VERSION;
