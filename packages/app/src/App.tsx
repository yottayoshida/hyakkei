// Placeholder editor shell. Real editor (file load, chart builder, guideline
// nudges, grid layout) lands in M2 (issues #11-#16).
export function App() {
  return <p>Hyakkei — under construction (M2).</p>;
}
