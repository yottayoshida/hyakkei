// Placeholder for single-file/folder packaging (ADR-0005, ARCHITECTURE §4/§9).
// Real content lands in M3 (issues #17, #18); later reused by the CLI (v0.4).
import { CORE_PACKAGE_VERSION } from "@hyakkei/core";

export const EXPORT_PACKAGE_VERSION = CORE_PACKAGE_VERSION;
